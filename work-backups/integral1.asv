h=input('De el valor de h: ');
suma=0;
 x=linspace(1,5,51);
 y=x;
 f=x.^2+y.^2;
 m=numel(x);
 n=numel(y);
 for i=1:
    for j=1:n
     suma=suma + f(i,j)*h*h;
    end
 end 
 m
 n
 suma

 