%Familia de rectas
syms x;
m=input('pendientes= ');
b=input('interceptos con y =');
hold on;
for i=1:5
    disp(['ecuación #',num2str(i),':']);
    y=m(i)*x+b(i)
    ezplot(y)
end
title('Familia de rectas');