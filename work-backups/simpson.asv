function s=simpson(f,a,b,n)
clc;
x=linspace(a,b,n+1);
g=inline(f);
suma=0;fact=(b-a)/(3*n);
for i=2:n-1
    if isodd(i)==0
        ter=4*g(i)       
    else
        ter=2*g(i)
    end
    suma=
end

s=fact*(g(a)+g(b)+suma);
    