%introduccion de vertices del poligono
n=input('deme el No. de vertices del poligono ');
disp('introduzca las coordenadas de los vertices x(i),y(i)')
for i=1:n
    fprintf('x(%d)=',i)
    fprintf('(%d)=',i)
    x(i)=input('');
end
x