%PRUEBA 1, productos
sumtotal=0;
while 1
    clc
    compra=0;
    nombre=input('¿Cual es el nombre del empleado?: >> ','s');
    while
        precio=input('De la nota >> ');
        if precio==0
            break;    
        else
            compra=compra+precio;
        end
    end
    fprintf('Nombre del cliente:'