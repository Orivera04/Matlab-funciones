%Traslaciones M: matriz de gráfica original.

x=input('Introducir dominio de la gráfica x=');
y=input('Introducir función f(x)=');
 T=input('Dar vector de traslación [h,k]=');
 h=T(1);
 k=T(2);
 x1=x+h;
 y1=y+k;
 plot(x,y);
 hold on;
 plot(x1,y1,'r');
 

