function exit = help_msg(fig,help_msg_id)

% displays Return to Main Menu

exit = uimenu(fig, ...
              'Label', sprintf('Help'),...
              'Tag','exit', ...
              'Callback',sprintf('help_message(%i)',help_msg_id)) ;
