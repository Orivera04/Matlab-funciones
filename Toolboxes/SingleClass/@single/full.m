%Convert sparse matrix to full matrix.
%
%    Leutenegger Marcel © 26.11.2005
%
function o=full(o)
% if issparse(s)
%    o=zeros(single(size(s)));
%    [i,j,v]=find(s);          % no single sparse yet
%    o(i,j)=v;
% else
%    o=s;
% end
