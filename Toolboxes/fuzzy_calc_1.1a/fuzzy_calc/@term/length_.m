function p = length(v)
%Fuzzy summe of terms  class constructor.
%   p = term(a,d) creates a fuzzy object from the vector numerator a
%   and denumerator d.
% y(i) = varargin{i}(2);



p=length(v(:).n);
