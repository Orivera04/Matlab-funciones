function y=puntopen(m,b)
%Ecuación de la recta en la forma punto pendiente
global m b;
%Introducir la pendiente y el intercepto con eje y
m=input('pendiente= ');
b=input('intercepto= ');
y=inline('m*x+b','x');
end