%Grafo con lazos en cada vértice
%Intodcir los vértices del grafo:
v=input('1as. coordenadas de vértices: ');
w=input('2as. coordenadas de vértices: ');
n=numel(v);
axis([0 10 0 10]);
hold on;
for i=1:n
plot(0.2*cos(0:0.2:6.28)+v(i),0.2*sin(0:0.2:6.28)+w(i));
plot(v(i),w(i),'*r');
text([v(i),w(i)+0.4,'v(,num2str1','FontSize',12])
end
plot(v,w)