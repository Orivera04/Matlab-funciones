function mat = cartprod1(m, n)
% multtable returns a matrix which is a
% multiplication table
% Format: multtable(nRows, nColumns)
% Preallocate the matrix
mat=[];
for i = 1:m
for j = 1:n
    m1=num2str(i);
    m2=num2str(j);
    mat(i,j) = strcat(m1,',',m2)
end
end
end
