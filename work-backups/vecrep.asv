function M=vecrep(n)
%Vectores con sucesiones de elementos repetidos
clc;
matrep1=[];matrep2=[];
for i=1:n
    elrep=[i];
    matrep1=[matrep1,repmat(elrep,1,n-i)];
    fila2=[i+1:n];
    matrep2=[matrep2,fila2];
    
end
R1=matrep1;
R2=matrep2;
M=[R1;R2]';
