%roc.hlp
%
% Help file for workspace roc.mat
%
% Workspace containing data and parameters for roc example
% of Chapter 5
%
% Workspace contains:
%
% D - vector of indicators of disease status (1-yes, 0-no)
% N - the data matrix, with "items" rows and "raters" columns.
% TmT - vector of treatment indicators (0 or 1)
% alpha, lambda - values of hyperparameters of the inverse gamma prior on the variance
% sampSize - number of iterations of the MCMC simulation

