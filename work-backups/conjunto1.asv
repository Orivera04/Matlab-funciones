function SN=conjunto1(A,B)
%Determina si A es o no subconjunto de B.
ind=ismember(A,B);
s1=isstr(A);s2=isstr(B);
if s1==1 && s2==1 && all(ind)==1
    disp('A es subcsdena de B';
    SN=1;
elseif s1==1 && s2==1 && all(ind)==0
    disp('A  no es subcsdena de B';
    SN=0:
elseif s1==0 && s2==0  all(ind)==1
    disp('A es subconjunto de B');
    SN=1;
else
    disp('A no es subconjunto de B');
    SN=0;
end