function start_maxim()

% This is the starting point
% The directory that contains this module must already be in the MATLAB path

% Change to root directory for MAXIM
directory = pwd ;
eval(['cd ' directory])

% Set the path for other required directories (all exist under root Maxim directory)
if isempty(findstr(upper(path), 'MODULES'))
   addpath(sprintf('%s', directory), ...
          sprintf('%s\\gui', directory), ...
          sprintf('%s\\utils', directory), ...
          sprintf('%s\\utils\\gui', directory), ...
          sprintf('%s\\Modules\\control', directory), ...
          sprintf('%s\\Modules\\ctmm', directory), ...
          sprintf('%s\\Modules\\design', directory), ...
          sprintf('%s\\Modules\\dtmm', directory), ...
          sprintf('%s\\Modules\\gmm', directory), ...
          sprintf('%s\\Modules\\prob', directory), ...
          sprintf('%s\\Modules\\queue', directory) ) ;
end
Scale = 1 ;

% launch the main screen - control shifts to the maxim_main driver 
feval('maxim_main')